<!DOCTYPE html>
<html lang="en">
	<head>
		<% include ../partials/head %>
	</head>
	<body class="container">
		
		<header>
			<% include ../partials/header %>
		</header>
		
		<main>
			<div class="container page">
				<hr>
				<% if (user.setup === false) {
					%>
					<h1>Please setup account before you can search for likes</h1>
					<a class="nav-link" href="/profile">Profile</a>
					<%
				} else {
					%>
					<div class="card-group mr-auto ml-auto">
						<div class="w-100">
							<!--Cards-->
							<% 
								for (var i = 0; i < likes.length; i++) { 
									var date = new Date();
									var mydate = new Date(likes[i]['age']);
									var myAge = date.getFullYear() - mydate.getFullYear();
									if (myAge > 17) {
									%>
									<div class="card text-white bg-dark rounded" class="w-100">
										<div class="card-body">
											<h5 class="card-title"><%= likes[i]['username'] %></h5>
											<p class="card-text"><%= likes[i]['biography'] %></p>
											<a href="/view/<%= likes[i]['_id'] %>" class="btn btn-primary">View</a>
											<a href="/unlike/<%= likes[i]['_id'] %>" class="btn btn-primary">unLike</a>
											<a href="/block/<%= likes[i]['_id'] %>" class="btn btn-warning">block</a>
											<a href="/report/<%= likes[i]['_id'] %>" class="btn btn-danger">Report</a>
										</div>
									</div>
									<hr>
									<% 
									}
								}
							%>
							<hr>
							<!--End of Cards-->
						</div>
					</div>
					<% } %>
					<hr>
    </div>
</main>

<footer>
    <% include ../partials/footer %>
</footer>

</body>
</html>